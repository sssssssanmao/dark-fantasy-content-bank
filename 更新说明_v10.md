# 工作资料库 v10 更新说明

- 全站名称由“暗黑玄幻内容银行”统一调整为“工作资料库”。
- 修复手机和窄屏设备上导航菜单完全隐藏的问题。
- 增加首次登录令牌时间校验异常的自动恢复机制。
- 保留原有 Supabase 数据表、账号和历史内容，不需要重新运行数据库脚本。

## 更新方式

将本目录中的 `index.html`、`app.js`、`config.js`、`.nojekyll`、`supabase-schema.sql` 和 `README.md` 上传并覆盖 GitHub 仓库根目录。不要删除 Supabase 项目，也不要重新创建数据表。
