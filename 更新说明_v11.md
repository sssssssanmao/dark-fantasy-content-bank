# 工作资料库 v11 交互修复说明

## 本次修复

- 为总览页四张统计卡增加点击跳转。
- 为灵感原料列表增加整行点击编辑。
- 为右侧四项流水线状态增加点击跳转。
- 增加悬停反馈、键盘操作与无障碍标签。
- 保留新增、加工、编辑、删除、登录和云端同步逻辑。

## 更新方式

将本目录中的 `index.html`、`app.js`、`config.js`、`.nojekyll`、`supabase-schema.sql` 和 `README.md` 上传并覆盖 GitHub 仓库根目录。

本次不修改数据库结构，不需要重新运行 `supabase-schema.sql`，也不要删除 Supabase 项目或数据表。
